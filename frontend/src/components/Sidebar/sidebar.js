import styles from "./sidebar.module.css"

export default function Sidebar(props) {
  return (
      <div className={styles.sidebar}>
        <div className={styles.headers}>
        <h2 className={styles.header}>helo</h2>
        <h2 className={styles.header}>helo</h2>
        </div>
      </div>
  );
}